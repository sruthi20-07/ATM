# -----------------------------
# Server Settings
# -----------------------------
# IMPORTANT: On Render, the port must be taken from environment variable
server.port=${PORT:8056}
spring.application.name=ATM-Project

# -----------------------------
# Database Configuration (CLOUD READY)
# -----------------------------
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# -----------------------------
# Hibernate / JPA Settings
# -----------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Dialect will be auto-detected by MySQL driver, so no need to hardcode
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# -----------------------------
# SendGrid API Key (From Environment Variable)
# -----------------------------
sendgrid.api.key=${SENDGRID_API_KEY}

# -----------------------------
# Custom Email Info
# -----------------------------
email.from.address=vindulasruthi20@gmail.com
email.from.name=SBI ATM Service

# -----------------------------
# Spring Security (Basic Auth)
# -----------------------------
spring.security.user.name=${APP_USER:user}
spring.security.user.password=${APP_PASSWORD:sruthi123}
spring.security.user.roles=USER

# -----------------------------
# Logging
# -----------------------------
logging.level.org.springframework=INFO
logging.level.com.atm=DEBUG

